# torch ставится ОТДЕЛЬНО (CPU-сборка) — см. Dockerfile / setup_local.sh,
# чтобы не тянуть огромные CUDA-колёса. Здесь — всё остальное.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
openai==1.59.9
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
transformers==4.47.1
# КРИТИЧНО: numpy<2 — PyTorch скомпилирован под NumPy 1.x.
numpy<2
soundfile==0.12.1
